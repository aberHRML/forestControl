language: r
warnings_are_errors: false
sudo: true

r:
 - release
 - devel

r_packages:
 - testthat
 - covr
 - randomForest
 - ranger

after_success:
 - Rscript -e 'library(covr); codecov()'

notifications:
  email:
   on_success: change
   on_failure: change
